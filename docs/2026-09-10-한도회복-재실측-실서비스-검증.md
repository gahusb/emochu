# 한도 회복 점검·AI 재실측·배포 모바일 화면 검증

2026-09-10 KST, 사용자 요청에 따른 검사 기록. **관광 상세 조회는 이번 표본에서 정상화됐지만, Gemini는 실제 코스 생성 중 429가 재발했다. 실제 휴대폰 검증은 아직 완료하지 못했다.** 로컬 검사 통과와 배포·실기기 검증을 구분한다.

후속 9/11: [iPhone 17 수동 검증 결과](2026-09-11-iPhone17-수동검증-결과.md). 사용자가 기본 조작에 특이사항 없음을 보고했고 사진 5장을 확인했다. 아래 실기기 대기 표는 **9/10 당시 기록**이며, 최신 확인 범위는 후속 문서를 따른다. 확대된 홈 사진을 확인했으므로 `maximum-scale=1` 관측을 해당 iPhone의 실제 확대 불가로 해석하지 않는다. AI 생성 이후 저장·공유는 여전히 별도다.

## 1. 한도 회복 판정

10:24 최소 요청으로 관광 상세 GET 1회와 작은 Gemini 생성 POST 1회를 실행했다. 자동 재시도, 계정 설정 변경, DB 기록은 하지 않았다.

| 확인 요청 | 관측 결과 | 해석 |
|---|---|---|
| 관광 `detailIntro2`, 음식점 1곳 | HTTP 200, 기관 코드 0000, 해당 ID 1건, 운영시간 원문 있음, 200ms | 이 요청의 상세 조회 정상 |
| Gemini `gemini-3.6-flash`, 짧은 JSON 응답 | HTTP 200, 정상 JSON, STOP, 1,296ms, 입력 14/출력 5토큰 | 작은 생성 요청 성공. 코스 생성 한도 회복의 증거는 아님 |
| 이어서 서울 실제 코스 생성 | HTTP 200, AI 코스 완성 | 본문이 큰 요청도 1회 성공 |
| 이어서 강릉 실제 코스 생성 | HTTP 429, quota 오류 | 지속적인 코스 생성 가능 상태는 확인하지 못함. 이후 AI 전송 중단 |

잔여량·리셋 시각·분당 요청/토큰/일일 한도 중 어느 제한인지는 확인하지 못했다. 날짜 변경이나 작은 요청 성공만으로 한도 회복을 선언하지 않는다. 로컬 실측용 키와 배포 환경의 키가 같은지도 확인하지 않았다.

최초 샌드박스 실행의 `EACCES`는 로컬 네트워크 권한 오류이며 기관의 한도 응답으로 집계하지 않았다. 허용된 외부 실행에서 위 결과를 확인했다.

근거: `.scratch-live-course/recovery-2026-09-10T01-24-36-391Z.json`. 로컬 권한 오류 기록은 `recovery-2026-09-10T01-24-15-983Z.json`으로 따로 보존했다.

## 2. AI 재실측 — 24조건 중 배치 6만 실행

10:25~10:26, `node scripts/verify-live-course.mjs --matrix 6`. 이전 AI 미시도 9조건 중 가족·느긋하게·일요일의 서울/강릉/울릉도 3조건을 검사했다. 조건당 최초 AI 시도 최대 1회, 실행당 최대 3회이며 429 관측 후 후속 AI 요청을 차단한다. 모의 사용자 조건에 실제 관광·날씨·AI 응답을 사용한 **로컬 파이프라인 검사**이지 배포 서버의 전체 생성·저장 검사는 아니다.

| 조건 | 최종 결과 | 실제 AI 전송 | 전체 조건 처리 시간 | 알려진 원본·일정 검사 | 운영시간 미확인 |
|---|---|---|---:|---|---:|
| 서울 | AI | 200 성공 | 12,024ms | 통과 | 3/4곳 |
| 강릉 | 규칙 대체 | 429 실패 | 1,983ms | 통과 | 3/4곳 |
| 울릉도 | 규칙 대체 | 미전송: 앞선 429로 차단 | 1,597ms | 통과 | 1/4곳 |

- 실제 코스 AI 전송 **2회: 성공 1회 / 429 1회**. 울릉도는 AI 실패나 성공이 아니라 미시도다. 작은 JSON 확인 요청은 코스 성공 수에 포함하지 않는다.
- 배치 7·8은 실행하지 않았다. 이전 AI 미시도 9조건 중 이번에 실제 시도한 것은 2조건뿐이므로 **7조건은 여전히 AI 미시도**다. 강릉은 별도로 성공 확인이 남았다.
- 3개 최종 일정, 12개 방문 지점에서 원본 ID·이름·좌표 불일치 0, 알려진 날짜 위반 0, 중복 0, 판정 가능한 운영구간 불일치 0이었다. **운영시간은 7/12 미확인**이므로 전부 영업 가능한 코스라는 뜻이 아니다.
- 요청한 시각·청각 접근성 정보가 모두 있는 지점은 **0/12**였다. 정보 부족을 ‘접근 불가’ 또는 ‘접근 가능’으로 해석하지 않는다.
- 방문일 예보 범위는 세 조건 모두 확보했다. 실시간 예보·운영·예약·실제 이동시간 보장은 아니다.
- 조건별 시간은 관광·날씨 수집 등을 포함한다. AI/규칙이 섞인 작은 표본의 시간을 운영 P50/P95나 AI 응답 속도로 홍보하지 않는다.

### 외부 응답 집계

본 배치 실제 네트워크 108회 중 관광·날씨 105회는 HTTP 200이었다. 관광 소개 상세 `detailIntro2`는 **66/66 HTTP 200**이다. 나머지 3회는 모델 목록 GET 1회와 AI 생성 POST 2회다. 성공한 코스 생성의 토큰은 입력 4,438 / 출력 1,722 / 총 6,160이었다.

상세 조회가 정상이어도 운영시간 원문 부재·복합 조건 등으로 미확인이 남았다. 따라서 ‘429만 풀리면 운영시간 모두 확인’이라고 판단하지 않는다. 이번 결과를 캐시 도입의 트래픽 감소율이나 9/9와 비교한 운영 성공률로 사용하지 않는다.

근거:

- `.scratch-live-course/report-2026-09-10T01-25-51-198Z.json`
- `.scratch-live-course/matrix-summary-2026-09-10T01-29-08-165Z.json`
- 집계 명령: `node scripts/summarize-course-matrix.mjs report-2026-09-10T01-25-51-198Z.json`
- 날짜가 다른 기존 24조건 보고서는 보존하며 같은 날의 신규 AI 표본처럼 합산하지 않았다.

## 3. 배포 사이트의 실제 모바일 폭 검사

대상: `https://emochu.vercel.app`. 10:29~10:56 인앱 Chromium 브라우저, 주 검증 크기 **390×844**. 로컬 모의 응답이 아닌 배포 사이트를 열었다. 위치는 기본 서울을 사용했고 실제 GPS 권한·로그인·공개 게시·전화·예약은 실행하지 않았다.

| 항목 | 관측 결과 | 판정 범위 |
|---|---|---|
| 홈 | 9/12~13 주말·서울, 실제 날씨와 관광 추천·축제 안내 표시 | 데이터 표시 확인, 각 추천 품질 전수 검사는 아님 |
| 커뮤니티 | 0개, 빈 결과 안내 표시 | 기존 공개 코스가 없어 결과 저장·공유 검사는 진행 못 함 |
| 축제 목록 | 전체 34개 → ‘이번 주말’ 17개 | 키보드 Enter로 필터 변경 확인. 종료된 8월 축제는 주말 필터 결과에서 제외 |
| 축제 상세 | ‘덕수궁 밤의 석조전’ 사진 8장, 기간·공연시간·요금·주소·외부 링크 표시 | 렌더링 확인, 행사 운영·예약 사실 보증 아님 |
| 상세 지도 | Kakao 실제 지도 타일·장소 마커를 화면에서 확인 | 모바일 앱 길찾기 전환은 미실행 |
| 상세 닫기 | Escape로 닫고 ‘이번 주말’ 필터 유지 | 모달 복귀 확인 |
| 코스 입력 | 도시 서울 → 힐링 → 사주 건너뛰기 → 하루·가족 → 자연 취향 | 4단계 이동, 필수 입력 전 버튼 비활성/입력 후 활성 확인. **코스 만들기는 누르지 않음** |
| 가로 넘침 | 측정한 축제 목록·상세·입력 상태에서 문서 폭 ≤390px, 상세 창 내부 폭 390px | 측정 상태의 문서 가로 넘침 없음. 모든 기기 통과 아님 |

일부 클릭은 선택 변화가 확인되지 않아 키보드로 검사를 이어갔다. 자연 취향 입력 중 도구의 `Input.dispatchKeyEvent` 시간 초과가 한 번 있었으나 새 화면을 읽어 선택 및 생성 버튼 활성 상태를 확인했다. 입력 도구 문제와 제품 결함을 구분하며, **터치 탭 성공을 주장하지 않는다**. 종료 시 검사 viewport를 원복하고 검증용 탭을 닫았다. 화면은 도구 출력으로 육안 확인했으며 이번 배포 검사에 별도 PNG 파일 묶음을 만들지는 않았다.

### 확인된 배포 차이와 남은 수정안

| 우선순위 | 근거 | 필요한 조치 |
|---|---|---|
| P1 | 배포 viewport에 `maximum-scale=1` 존재. 현재 로컬 `app/layout.tsx`에는 없음 | 승인된 배포 후 해당 메타 제거 확인. 실제 폰 핀치 확대 별도 검사 |
| P1 | 배포 축제 반경 버튼 높이 약 29.3px. 로컬 `FestivalRadius.tsx`에는 `min-h-11` 적용 | 최신 빌드 반영 확인 후 44px 이상 목표를 배포 DOM과 폰 터치로 재검사 |
| P1 | 배포 입력 화면은 ‘문 여는 곳만’이라고 단정. 로컬 `StepWhenWho.tsx` 91행에도 ‘그날 문 여는 곳으로만 짜드려요.’가 남음 | 수정안: ‘방문일의 휴무·운영시간 정보를 참고해요. 미확인 정보는 결과에서 알려드려요.’로 바꾸고 표시 회귀 검사 |

앞의 두 항목으로 **현재 로컬 수정본과 배포 화면의 차이**를 확인했다. 배포 커밋·키·마이그레이션 일치 여부까지 확인한 것은 아니며, 전체 최신 변경이 미배포라고 단정하지 않는다. 마지막 문구는 **배포만 다시 해도 해결되지 않는 로컬 잔여 항목**이다. 이번 요청은 검증 단계이므로 앱 소스 수정·커밋·푸시·배포·DB 마이그레이션은 실행하지 않았다.

## 4. 실제 휴대폰 검증 — 대기

이 세션에서 제어 가능한 표면은 인앱 브라우저뿐이었다. `adb`·`idevice_id` 명령과 기본 Android SDK의 adb 경로도 확인되지 않았다. 이는 폰이 물리적으로 연결되지 않았다는 증명이 아니라 **현재 사용 가능한 폰 제어 경로가 없다는 뜻**이다. 기종/연결 가능 여부를 사용자에게 질문했으며 응답 대기 중이다.

아래 표는 실행 기록이 아닌 **실기기 검사표**다. 기종·OS·브라우저 버전·배포 URL·검사 시각과 함께 결과를 채운다.

| 순서 | 사용자 폰에서 확인할 항목 | 완료 기준 | 현재 |
|---|---|---|---|
| 1 | 홈·축제 화면을 두 손가락으로 확대/축소 | 확대 가능, 본문·버튼 잘림 없음 | 미검증 |
| 2 | ‘이번 주말’ 필터와 반경 버튼 터치 | 선택 표시와 결과가 바뀌고 오탭 없음 | 미검증 |
| 3 | 축제 상세 열기·스크롤·기기 뒤로가기 | 닫힘/복귀 정상, 목록 필터 유지 | 미검증 |
| 4 | 상세 지도 이동·확대, 카카오맵 링크 | 지도 정상, 앱/웹으로 올바른 장소 전달 | 미검증 |
| 5 | 위치 거절 후 서울 기본 경로, 필요시 직접 위치 허용 | 거절해도 이용 가능, 허용 시 위치 표시 일치 | 미검증; 권한 결정은 사용자 |
| 6 | 도시→사주 건너뛰기→날짜/동반자→취향 | 키보드·하단 버튼 가림 없이 진행 | 미검증 |
| 7 | 기존 비공개 코스 저장·새로고침·공유 URL 수신 | 내용 유지, 타 기기 조회, 방문자는 편집 불가 | 미검증; 사용할 기존 코스 필요 |
| 8 | 한도 회복 확인 후 신규 AI 코스 1회 | AI/규칙 구분, 운영시간 미확인, 지도·저장 동작 | 미검증; 현재 AI 추가 호출 중단 |

운영 코스를 공개로 게시할 필요는 없다. 개인 위치·출생연도·API 키·편집 토큰이 보이는 화면은 제출 증빙으로 공유하지 않는다.

## 5. 로컬 검사와 다음 진행 순서

**10:51 release-green GREEN:** 테스트 500 통과, ESLint·Next 빌드 통과, 리포트 비밀값 대조 18건/유출 0건. 근거 `loops/release-green/outputs/green-2026-09-10-1051.md`. 외부 API 가용성이나 배포·실기기 통과를 뜻하지 않는다.

이번 검사 지원 변경은 `scripts/check-live-recovery.mjs` 추가와 `scripts/live-course.probe.ts`의 `hoursStatus` 기록 추가다. 회복 확인기는 관광 상세 최대 1회+작은 AI 생성 최대 1회이며 DB·재시도는 없다. **프로세스 종료 코드만 보지 말고 보고서의 서비스별 `usableResponse`를 확인**한다.

다음 작업:

1. 계정 소유자가 Gemini 실제 제한 항목·잔여량/해제 시각을 확인한다. 키·결제·한도 설정은 변경하지 않는다. 확인 없이 배치 7·8을 재호출하거나 자동 재시도를 예약하지 않는다.
2. 잔여 과장 문구 수정·검증 후 승인된 배포를 진행하고, viewport·버튼 크기·운영시간 표시가 실제 URL에 반영됐는지 재검사한다. DB 마이그레이션 상태도 승인된 배포 점검에서 확인한다.
3. 실제 폰의 위 검사표를 채운다. AI가 막혀 있어도 기존 비공개 코스와 장소 상세로 지도·저장·공유 대부분을 먼저 확인할 수 있다.
4. 제출 문서에는 ‘이번 표본: AI 생성 1건 성공, 규칙 대체 2건, 운영시간 7/12 미확인’처럼 분모와 한계를 함께 쓴다. 500개 단위 테스트·62장 이전 모의 캡처·이번 실제 API 표본·실기기 결과를 서로 대체하지 않는다.

이전 기록: [상세 조회 보호·운영시간 표시 개발](2026-09-10-상세조회-한도-운영시간-보완.md), [24조건 검증](2026-09-09-24조건-검증-개발결과.md).
