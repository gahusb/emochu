# 카카오톡 코스 공유 수정·운영 배포 결과

2026-09-12 13:30 KST 확인. 사용자가 공유 수정만 분리한 커밋·푸시·기존 Vercel 운영 배포를 승인했다.

## 결과

**상대 경로를 그대로 공유하던 결함을 수정하고 운영 반영을 확인했다.** `SaveShareBar`가 API의 `/course/{slug}`를 현재 운영 도메인을 포함한 전체 주소로 변환한다. 카카오 콘텐츠 링크와 ‘코스 보기’ 버튼의 모바일/PC 주소, 링크 복사에 모두 적용했다. 클립보드 권한이 없으면 직접 선택·복사할 주소 입력란을 표시한다.

실제 iPhone 카카오톡의 새 메시지 클릭은 아직 사용자 확인 전이다. 기존에 보낸 메시지의 잘못된 링크가 이번 배포로 교정된다고 가정하지 않는다.

## 배포 범위와 식별 정보

- 저장소: `gahusb/emochu`, 기존 `main` 브랜치.
- 커밋: `075b160aa1166da288d8c1657aaa9f6c0d45da96` (`fix(share): use absolute course URLs for Kakao and clipboard`).
- 커밋 파일: `app/components/course/result/SaveShareBar.tsx`, `tests/kakao-share.test.ts` 두 개만 포함.
- [Vercel 배포](https://vercel.com/gahusbs-projects/emochu/9w25iD6bnSP1LTpcKmCTRSCzN7Jg): GitHub 커밋 상태 `Vercel / success / Deployment has completed` 확인.
- 운영 주소: [emochu.vercel.app](https://emochu.vercel.app). 배포 성공 상태 외에 이 도메인에서 제공하는 실제 공유 JS도 별도로 확인했다.
- 이전 커밋: `5269e3feaa6a56c25fdef1a401a88ebf523069be`. 이전 [Vercel 배포](https://vercel.com/gahusbs-projects/emochu/6PSb3i8KMd9geqaTaNo1ewSj4cwn)를 복구 판단용으로 기록한다. 롤백은 실행하지 않았다.

누적 미커밋 코드·문서·개인 설정·환경 파일은 이번 커밋에 넣지 않았다. DB 권한·Kakao 앱 설정·Vercel 환경변수도 변경하지 않았다. 서버 admin의 anon 폴백 제거 등 다른 로컬 개발이 함께 배포된 것은 아니다.

## 배포 소스의 격리 검증

기존 HEAD를 임시 디렉터리에 추출하고 공유 수정과 테스트만 적용했다. 잠금 파일대로 의존성을 설치하고, 이 소스와 실제 커밋 대상 두 파일의 SHA-256 일치를 확인했다. 운영 비밀값은 복사하지 않았다.

- Vitest: **221 통과 / 4 건너뜀**, 26개 파일 통과 / 2개 파일 건너뜀. 건너뛴 항목은 키가 필요한 관광 API 실호출 검사다.
- 공유 회귀 테스트 **8개** 포함: 상대/전체 주소의 카카오 카드·버튼 링크, 전체 주소 복사, SDK 없음/초기화 전 복사 대체, 복사 거부 시 직접 복사 안내와 재시도, 보존 요청 실패 시 공유 유지, 초기 서버 렌더 시 window 접근 방지.
- 테스트는 실제 컴포넌트 핸들러를 호출하되 React 상태·Kakao SDK·클립보드·fetch·타이머를 모킹한다. 실제 카카오 메시지를 보내는 종단간 검사는 아니다.
- ESLint: **오류 0 / 경고 10**. 공유 변경 파일에는 경고 없음. 기존 effect/hook/미사용 변수 경고는 이번 범위에서 수정하지 않았다.
- Next.js 운영 빌드·TypeScript 검사 성공. 기존 middleware 규약 폐기 예정 경고는 남아 있다.
- `git diff --cached --check` 통과, 파일 두 개만 스테이징했음을 확인 후 커밋·비강제 push.

이 결과는 **공유 수정만 담은 배포 소스**의 검사다. 누적 개발 작업 트리의 9/11 22:18 `516 tests GREEN` 기록과 분모가 다르며, 전체 release-green 루프를 재실행하거나 그 기록을 갱신한 것이 아니다.

## 운영 반영 확인

2026-09-12T04:30:34.038Z부터 사용자 제공 기존 코스 표본을 확인했다. 실제 slug·제목·내용·좌표·토큰 값은 문서에 기록하지 않는다.

- 코스 HTML: HTTP 200, 최종 코스 경로 유지.
- 해당 HTML에서 발견한 JS: `/_next/static/chunks/0lwnlxu1xz4im.js`, HTTP 200.
- 이 청크에서 `new URL(shareUrl, window.location.origin).href`에 해당하는 변환과 직접 복사 안내 확인. 카카오 콘텐츠·버튼의 `mobileWebUrl`/`webUrl` 및 클립보드가 변환 함수를 호출한다. 이전 청크의 상대 주소 직접 전달과 구분했다.
- 코스 조회 API: HTTP 200, 코스 존재, 상대 shareUrl 응답 유지, 응답 최상위 `editToken`/`edit_token` 필드 없음.

확인은 기존 코스 HTML 1회·공개 JS 읽기·조회 API 1회로 수행했다. 일반 조회수 증가와 페이지 메타데이터의 서버 조회는 발생할 수 있다. 새 코스 생성·편집·공개 토글·실제 메시지 발송·AI/관광 API 직접 호출·DB 권한 변경은 하지 않았다. 최상위 토큰 필드 확인은 전체 응답/DB 보안 감사가 아니다.

## 사용자 최종 확인

1. iPhone에서 기존 코스 화면을 새로고침한다. 새 코스를 생성할 필요는 없다.
2. ‘카카오톡 공유’를 눌러 **새 메시지**를 보낸다.
3. 새 메시지의 ‘코스 보기’를 눌러 같은 코스가 열리는지 확인한다.

새 메시지도 홈으로 이동하면 실제 메시지의 목적지·리다이렉트와 Kakao 제품 링크 도메인 설정을 추가로 점검한다. 편집 토큰이나 API 키를 요청하거나 DB 익명 권한을 다시 열지 않는다.
